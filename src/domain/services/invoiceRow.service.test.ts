import { InvoiceRowService } from './invoiceRow.service';

jest.mock('path');

describe('InvoiceRowService', () => {
  it('should import csv file', () => {
    // const csvReader: ICSVReader = jest.fn();
    // const invoiceRowService = new InvoiceRowService(csvReader);
  });
});
